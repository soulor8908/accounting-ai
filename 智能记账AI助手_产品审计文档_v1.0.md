# 智能记账AI助手 — 产品审计文档

> **文档版本**：v1.2（基于代码数据模型 v2.0 + 2026-07-30 体验调优复核）  
> **审计日期**：2026-07-30（初版 2026-07-24）  
> **审计对象**：基于自然语言的AI记账本系统（System Prompt v1.0 / 数据模型 v2.0）  
> **审计维度**：功能完整性、数据模型合理性、交互体验、安全合规、技术可行性
>
> **文档状态**：v1.0 中「支付宝 / 花呗 缺失」等结论已过时——代码 `src/core/types.ts` 已迭代到 v2.0，账户类型含 `wallet / alipay / cash / debit / credit / loan / installment`（花呗/白条归入 `installment`）。v1.2 复核进一步对齐 2026-07-30 的体验调优：账户/流水/记忆统一左滑操作、流水默认本月、默认模型切换为 DeepSeek、移除国际化压缩体积。改进路线图见 [改进路线图](./accounting-ai-analysis/改进路线图.md)。

---

## 一、产品概述

### 1.1 产品定位
通过自然语言交互（NLI）驱动的个人财务管理助手，覆盖收支记录、多账户管理、负债追踪（贷款/信用卡/白条）、分期计划、日历视图等通用记账本核心能力。

### 1.2 目标用户
- 有日常记账习惯但嫌传统APP操作繁琐的个人用户
- 持有多张银行卡、信用卡、有贷款/分期需统一管理的用户
- 偏好"说话即记账"的AI原生交互体验用户

### 1.3 核心价值主张
> "一句话完成记账，AI自动分类、算余额、管负债、提醒还款"

---

## 二、功能审计

### 2.1 功能清单与覆盖度

| 模块 | 功能点 | 需求覆盖 | 优先级 | 审计结论 |
|------|--------|----------|--------|----------|
| **基础记账** | 收入记录 | ✅ 已覆盖 | P0 | 通过 |
| | 支出记录 | ✅ 已覆盖 | P0 | 通过 |
| | 转账记录 | ✅ 已覆盖 | P0 | 通过 |
| | 自然语言解析（口语化金额） | ✅ 已覆盖 | P0 | 通过 |
| **账户体系** | 微信零钱 | ✅ 已覆盖 | P0 | 通过 |
| | 多张储蓄卡 | ✅ 已覆盖 | P0 | 通过 |
| | 多张信用卡 | ✅ 已覆盖 | P0 | 通过 |
| | 现金账户 | ✅ 已覆盖（v2.0 AccountType 含 `cash`） | P0 | 通过 |
| | 支付宝余额 | ✅ 已覆盖（v2.0 AccountType 含 `alipay`） | P0 | 通过 |
| | 理财账户（基金/股票） | ❌ 未提及 | P2 | 建议二期 |
| **负债管理** | 多笔贷款记录 | ✅ 已覆盖 | P0 | 通过 |
| | 贷款月供计算 | ✅ 已覆盖 | P0 | 通过 |
| | 信用卡账单追踪 | ✅ 已覆盖 | P0 | 通过 |
| | 信用卡分期 | ✅ 已覆盖 | P0 | 通过 |
| | 京东白条 | ✅ 已覆盖 | P0 | 通过 |
| | 白条分期 | ✅ 已覆盖 | P0 | 通过 |
| | 花呗/抖音月付 | ⚠️ 花呗已覆盖（归入 `installment` 信用分期账户）；抖音月付未独立支持 | P1 | 花呗通过，抖音月付建议二期 |
| | 亲友借贷 | ⚠️ 未明确 | P1 | 建议补充 |
| **日历视图** | 每日收支展示 | ✅ 已覆盖 | P0 | 通过 |
| | 总负债展示 | ✅ 已覆盖 | P0 | 通过 |
| | 每月应还聚合 | ✅ 已覆盖 | P0 | 通过 |
| | 账单日/还款日提醒 | ✅ 已覆盖 | P0 | 通过 |
| | 月度收支统计 | ✅ 已覆盖 | P0 | 通过 |
| **查询统计** | 账户余额查询 | ✅ 已覆盖 | P0 | 通过 |
| | 流水明细查询 | ✅ 已覆盖 | P0 | 通过 |
| | 分类统计 | ⚠️ 提及category但未定义统计规则 | P1 | 建议细化 |
| | 趋势分析（月度对比） | ❌ 未实现 | P1 | 建议优先（"消费分析"Agent 依赖此能力，见改进路线图 P1-1） |

**覆盖度评分**：核心功能覆盖率约 **95%**（代码 v2.0 已补齐支付宝、现金、花呗等账户类型）；剩余缺口为抖音月付、理财账户、趋势分析等 P2 项。

---

### 2.2 功能深度审计

#### 2.2.1 自然语言解析（NLP）

| 测试用例 | 期望行为 | 当前支持 | 风险等级 |
|----------|----------|----------|----------|
| "中午吃了碗面25" | 支出¥25，餐饮-午餐 | ✅ | 低 |
| "3k工资到账" | 收入¥3000，工资 | ✅ | 低 |
| "从招行卡转了两千五到微信" | 转账¥2500 | ✅ | 低 |
| "三十五块五" | ¥35.50 | ✅ | 低 |
| "昨晚请同事吃饭花了三百多" | 支出¥300+，需确认精确金额 | ⚠️ 未定义模糊金额处理 | **中** |
| "还了信用卡一部分" | 需追问具体金额 | ✅ | 低 |
| "买了部手机，招行信用卡分12期" | 创建分期计划+首笔记录 | ✅ | 低 |
| "昨天" / "上周三" / "7月15号" | 日期解析 | ✅ | 低 |
| "每月10号还房贷" | 周期性记账/自动记账 | ❌ 未支持 | **高** |
| "退款到账" | 需关联原交易冲正 | ❌ 未支持 | **中** |

**审计意见**：模糊金额、周期性交易、退款冲正三类场景缺乏处理机制，建议补充。

#### 2.2.2 贷款与分期计算

当前数据模型支持：
- 贷款本金、利率、期数、月供、剩余本金
- 分期总金额、期数、已还期数、月供、下期还款日

**潜在问题**：
1. **利率类型未区分**：等额本息 vs 等额本金 vs 先息后本，模型仅存储单一`monthly_payment`，无法自适应不同还款方式
2. **提前还款**：未支持提前还款后的月供重算
3. **浮动利率**：房贷LPR调整场景未覆盖
4. **信用卡最低还款**：仅支持全额还款，未支持最低还款及利息计算

**建议**：在`loan`对象中增加`repayment_method`字段（enum: 等额本息/等额本金/先息后本/自由还款）。

#### 2.2.3 日历视图

当前设计输出文本型日历，包含：
- 每日收支明细
- 待还款项提醒
- 月度汇总

**缺失能力**：
- 未来日期预算规划（"这个月还剩多少可支配"）
- 还款日倒计时/逾期提醒
- 收支趋势可视化（需图表支持，纯文本受限）

---

## 三、数据模型审计

### 3.1 账户模型（Account）

```json
{
  "id": "wechat",
  "name": "微信零钱",
  "type": "电子钱包",
  "balance": 0.00,
  "currency": "CNY"
}
```

| 字段 | 必要性 | 审计意见 |
|------|--------|----------|
| `id` | ✅ 必须 | 建议增加命名规范（如`cc_`前缀） |
| `name` | ✅ 必须 | 允许用户自定义，合理 |
| `type` | ✅ 必须 | 当前枚举：电子钱包/储蓄卡/信用卡/贷款/信用分期。建议增加：支付宝/现金/理财/投资 |
| `balance` | ✅ 必须 | 储蓄卡/电子钱包为正，信用卡为已用额度（负数或正数需约定） |
| `currency` | ⚠️ 可选 | 个人记账通常单币种，可保留扩展性 |
| `limit` | ⚠️ 信用卡需 | 仅信用卡/白条需要，建议拆分子类型模型 |
| `bill_day/due_day` | ⚠️ 信用卡需 | 同上，储蓄卡无需此字段 |
| `principal/rate/term` | ⚠️ 贷款需 | 同上，导致模型字段冗余 |

**问题**：单一Account模型承载差异极大的账户类型，字段冗余且类型安全弱。

**建议**：采用**继承模型**或**JSON Schema组合**：
- 基础账户：id, name, type, balance, currency
- 信用卡扩展：limit, bill_day, due_day, temp_limit
- 贷款扩展：principal, rate, term_months, start_date, repayment_method
- 分期扩展：total_limit, available_limit

### 3.2 流水模型（Transaction）

```json
{
  "id": "tx_001",
  "date": "2026-07-24",
  "time": "08:53",
  "type": "支出",
  "amount": 35.00,
  "account": "wechat",
  "category": "餐饮",
  "subcategory": "早餐",
  "description": "楼下包子铺",
  "tags": ["日常", "现金"],
  "related_account": null,
  "installment": null
}
```

| 审计项 | 结论 | 说明 |
|--------|------|------|
| 时间精度 | ✅ | 支持date+time，满足回溯需求 |
| 交易类型枚举 | ⚠️ | 收入/支出/转账/还款/借贷/分期。建议增加：退款、手续费、利息、调账 |
| 多账户支持 | ⚠️ | 转账用`related_account`，但一笔交易涉及多账户分账（如AA收款）未支持 |
| 分期关联 | ⚠️ | `installment`字段仅存储期数信息，未关联到具体分期计划ID |
| 标签系统 | ✅ | 支持多标签，灵活 |
| 附件/图片 | ❌ | 未支持小票/发票照片关联 |
| 地理位置 | ❌ | 未支持消费地点记录 |

### 3.3 分期模型（Installment）

```json
{
  "id": "inst_001",
  "name": "iPhone 16 分期",
  "type": "信用卡分期",
  "total_amount": 9999.00,
  "term": 12,
  "paid_terms": 3,
  "monthly_payment": 833.25,
  "next_due_date": "2026-08-05",
  "account": "cc_001",
  "status": "进行中"
}
```

| 审计项 | 结论 |
|--------|------|
| 已还期数手动维护 | ⚠️ **高风险**：依赖用户或AI每次还款后更新，易出错 |
| 无自动扣款关联 | ⚠️ 无法标记"已自动扣款" vs "待手动还款" |
| 手续费/利息分离 | ❌ 未支持，信用卡分期通常有手续费 |
| 提前结清 | ❌ 未支持 |

**建议**：增加`auto_deduct`（是否自动扣款）、`fee`（总手续费）、`parent_tx_id`（关联原始消费流水）。

---

## 四、交互体验审计

### 4.1 对话流程设计

当前规则要求每次回复包含4个部分：
1. 确认理解
2. 执行结果（JSON）
3. 当前概况
4. 下一步提示

**优点**：
- 结构清晰，用户能明确知道AI理解了什么
- JSON输出便于前端解析和持久化

**风险**：
1. **JSON暴露给用户**：非技术用户看到JSON会感到困惑，建议折叠或仅展示可读文本
2. **过度确认**：每笔账都走4步流程，高频记账场景效率低
3. **追问时机**：规则要求"未明确的信息要追问"，但连续追问会降低体验，建议提供"智能默认值+一键确认"

### 4.1.1 列表项左滑操作（2026-07-30 新增）

账户、流水、记忆三处列表项已统一采用移动端原生左滑手势：

- **交互**：水平拖拽卡片向左滑动，露出右侧「编辑 / 删除」操作区（宽 132px），超过 40% 阈值吸附打开，否则回弹关闭。
- **排他**：模块级状态机保证同一时刻只有一行打开，打开新行自动关闭前一行，避免视觉混乱。
- **误触保护**：拖拽位移超过 4px 视为拖动而非点击，避免误触发行内点击；已打开的行点击内容区会自动收起。
- **实现**：通用组件 `src/ui/SwipeableRow.tsx`，pointer events 同时支持触摸与鼠标，桌面端亦可拖拽。
- **设计取舍**：相比常驻按钮，左滑减少了视觉噪音、把屏幕空间还给信息密度，符合移动端原生体验；代价是发现性略低（用户需知道可滑动），通过保留编辑态表单的明显入口与熟悉的手势约定缓解。

### 4.1.2 流水默认本月筛选（2026-07-30 新增）

进入流水页时，月份筛选器默认填充当前月份（YYYY-MM），用户进入即可看到本月流水，无需手动选择。仍可一键「清除」查看全部历史流水。这符合大多数记账场景的即时需求（"我这个月花了多少"），同时不破坏回溯能力。

### 4.2 异常处理

| 异常场景 | 当前处理 | 建议 |
|----------|----------|------|
| 余额不足（支出>余额） | 未定义 | 应警告并询问是否透支/借款 |
| 信用卡超额消费 | 未定义 | 应检查`limit`并提示 |
| 重复记账 | 未定义 | 应检测相似交易（金额+时间+描述）并提示 |
| 用户说"撤销" | 未定义 | 需支持最近一笔交易的撤销/回滚 |
| 账户不存在 | 追问 | 合理，但应提供快速创建选项 |

### 4.3 高频场景效率

**场景**：用户每天记5-10笔账
- 当前模式：每笔记账 = 输入 → AI解析 → 确认 → 展示结果 → 下一步提示
- 理想模式：输入 → AI静默记账 → 仅异常时打断 → 批量确认

**建议**：增加"快速记账模式"，连续输入多条记录后统一确认。

---

## 五、安全与合规审计

### 5.1 数据安全

| 项目 | 当前设计 | 审计意见 |
|------|----------|----------|
| 敏感信息存储 | 明确不存储银行卡号、密码 | ✅ 合规 |
| 交易数据加密 | 未提及 | ⚠️ 建议本地加密存储 |
| 云端同步 | 未提及架构 | ⚠️ 如后续增加，需考虑传输加密和隐私协议 |
| 数据导出 | 未提及 | P2 功能，但涉及用户数据所有权 |

### 5.2 金额安全

| 项目 | 当前设计 | 审计意见 |
|------|----------|----------|
| 大额交易提醒 | >¥50000提醒确认 | ✅ 合理 |
| 余额校验 | 未定义 | ⚠️ 转账/支出后应校验账户余额非负（信用卡除外） |
| 操作日志 | 未定义 | ⚠️ 建议记录增删改日志，便于审计 |

### 5.3 合规风险

- **金融建议边界**：提示词明确"不主动提供理财建议"，✅ 合规
- **信贷信息**：记录贷款/信用卡信息属于个人敏感信息，需符合《个人信息保护法》
- **数据留存**：如使用云端AI模型，需确认数据是否被模型训练留存

---

## 六、技术架构审计

### 6.1 AI模型选型建议

| 能力需求 | 要求 | 推荐模型 |
|----------|------|----------|
| 自然语言解析 | 高准确率提取金额、账户、类别 | GPT-4o / Claude 3.5 / GLM-4 |
| JSON结构化输出 | 严格Schema遵循 | 需配合Function Calling/JSON Mode |
| 中文口语理解 | "3k"、"三十五块五"、"昨晚" | 国内模型（GLM/通义）更优 |
| 数学计算 | 月供计算、余额更新 | 需验证模型计算准确性，建议前端二次校验 |

**默认模型（2026-07-30 调整）**：代码 `src/core/ai/config.ts` 的 `defaultConfig()` 已将默认服务商切换为 **DeepSeek**（`deepseek-v4-flash`），原因：国内访问稳定、CORS 友好、性价比高、合规风险低。Agnes 试用代理、MiMo、自定义 OpenAI 兼容 API 仍可在设置页切换。用户首次进入设置页即看到 DeepSeek 表单，仅需填入 API Key 即可开始使用。

### 6.2 状态管理策略

当前提示词要求AI维护完整账户状态，存在**严重风险**：

```
风险：AI幻觉导致余额错误
例：用户说"微信还有1000"，AI可能记成10000
```

**推荐架构**：

```
用户输入 → AI解析 → 输出操作指令（JSON）→ 前端执行 → 更新本地状态 → 下轮对话附带最新状态
```

即：**AI只负责"理解意图+生成指令"，不直接维护状态**。状态由客户端（前端/本地数据库）维护。

> **代码现状（2026-07-30 复核）**：已落实此架构。`src/core/engine/engine.ts` 作为指令引擎消费 AI 输出的 JSON 指令，`src/core/store/store.ts` 是唯一状态源；AI 不持有状态，仅生成指令，符合 P0 建议。

### 6.3 持久化方案

| 方案 | 适用场景 | 建议 |
|------|----------|------|
| 纯本地（IndexedDB/LocalStorage） | 单设备、隐私优先 | MVP首选，零后端成本 |
| 本地+云同步 | 多设备 | 需用户授权，加密上传 |
| 区块链/去中心化 | 极客用户 | 过度设计，不推荐 |

### 6.4 性能与体积守卫（2026-07-30 新增）

针对 Cloudflare 国内访问慢、对体积敏感的部署场景，项目已建立端到端体积管控：

- **代码分割**：`react-vendor` 单独成 chunk 长期缓存；`LockView` 等次级视图懒加载。
- **移除国际化**：i18n 模块已移除，文案硬编码中文，首屏 gzip 较带 i18n 版本下降约 5KB。
- **打包分析**：`npm run analyze` 通过 `rollup-plugin-visualizer` 生成 `dist/stats.html` treemap。
- **体积守卫**：`scripts/size-guard.mjs` 在构建后扫描 `dist/`，对首屏 gzip 总量与各关键 chunk 设定预算（首屏 ≈ 100KB、主包 ≈ 38KB、react-vendor ≈ 47KB、CSS ≈ 10KB），超出即非零退出。
- **CI 门禁**：`.github/workflows/ci.yml` 在 PR 上执行 `lint → test → build → size` 全链路，体积回退自动拦截。
- **加载态**：所有异步请求/懒加载视图均带 loading 反馈（`view-loading` 旋转占位、AI 测试连接的 `testing` 态、数据导入的 `dataBusy` 态），避免用户面对无响应界面。

---

## 七、竞品对比

| 维度 | 本产品（AI记账） | 随手记/挖财 | 钱迹 | 网易有钱（已停服） |
|------|-----------------|-------------|------|-------------------|
| 记账方式 | 自然语言对话 | 手动选择分类 | 手动+模板 | 自动同步银行 |
| 账户覆盖 | 微信/银行卡/信用卡/贷款/白条 | 全 | 全 | 全 |
| 负债管理 | AI自动计算月供 | 手动录入 | 手动录入 | 自动 |
| 使用门槛 | 极低（说话即可） | 中 | 低 | 低 |
| 数据隐私 | 高（本地优先） | 低（云端） | 高（本地） | 低 |
| 多币种 | 未支持 | 支持 | 支持 | 支持 |
| 价格 | 免费（AI Key成本） | 免费+广告 | 买断制 | 免费 |

**差异化优势**：自然语言交互的极低门槛 + 本地优先的隐私保护 + AI自动负债计算。

---

## 八、风险登记册

| 风险ID | 风险描述 | 概率 | 影响 | 缓解措施 |
|--------|----------|------|------|----------|
| R01 | AI解析错误导致记错账（金额/账户/方向） | 中 | 高 | 每笔展示确认；支持撤销；关键操作二次确认 |
| R02 | AI幻觉导致余额/负债数据失真 | 中 | 高 | AI不维护状态，仅生成指令；前端校验计算 |
| R03 | 用户忘记还款导致逾期 | 低 | 高 | 还款日前3天/1天主动提醒；日历视图高亮 |
| R04 | 分期手续费计算错误 | 中 | 中 | 提供手续费计算器；允许用户修正月供金额 |
| R05 | 数据丢失（本地存储） | 中 | 高 | 定期导出JSON备份；可选云同步 |
| R06 | 信用卡账单日/还款日记错 | 低 | 中 | 首次设置时确认；支持从银行短信自动识别 |
| R07 | 多账户间重复记账 | 中 | 中 | 相似交易检测；转账时双向校验 |

---

## 九、改进建议（按优先级排序）

### P0 — 阻塞发布
1. **状态管理重构**：AI不维护状态，仅输出操作指令，由前端执行并持久化
2. **增加撤销机制**：支持"撤销上一笔"、"修改刚才的记录"
3. **账户模型拆分**：信用卡/贷款/分期使用扩展模型，避免字段冗余

### P1 — 影响核心体验
4. **补充支付宝/花呗/抖音月付账户类型**：覆盖国内主流支付渠道
5. **增加还款方式区分**：等额本息/等额本金/先息后本
6. **增加周期性记账**：房贷/房租/工资等固定收支支持自动模板
7. **增加退款/冲正交易类型**：支持交易撤销和金额回退
8. **分期自动扣款标记**：区分"已自动扣款"和"待手动还"

### P2 — 体验优化
9. **快速记账模式**：连续输入多条，批量确认
10. **智能默认值**：基于历史数据预测账户和分类，减少追问
11. **收支趋势图表**：月度对比、分类占比饼图
12. **预算功能**：设置月度预算，超支提醒
13. **数据导出**：支持CSV/Excel导出，便于用户备份

### P3 — 长期规划
14. **银行短信解析**：自动识别银行消费短信并提示记账
15. **语音输入**：直接语音转文字记账
16. **多人账本**：家庭/合租场景AA记账

---

## 十、验收标准（MVP版）

| 编号 | 验收项 | 通过标准 |
|------|--------|----------|
| AC-01 | 自然语言记账 | 10种口语化表达（含模糊金额、时间）解析准确率≥90% |
| AC-02 | 多账户管理 | 支持≥5个账户（含微信/2张银行卡/1信用卡/1贷款），余额实时更新 |
| AC-03 | 负债追踪 | 录入贷款后自动计算月供；信用卡账单日/还款日可设置并提醒 |
| AC-04 | 分期管理 | 支持信用卡分期和白条分期，可查看剩余期数和下期应还 |
| AC-05 | 日历视图 | 可查看当月每日收支汇总及待还款项 |
| AC-06 | 数据持久化 | 刷新页面后数据不丢失（本地存储） |
| AC-07 | 撤销操作 | 支持撤销最近一笔交易并正确回滚余额 |
| AC-08 | 大额提醒 | 单笔≥50000元时弹出确认提示 |

---

## 十一、附录

### 附录A：推荐的数据模型 v2.0（优化版）

```typescript
// 基础账户
interface Account {
  id: string;
  name: string;
  type: AccountType; // wallet | debit | credit | loan | installment | cash
  balance: number;
  currency: string;
  createdAt: string;
  meta?: CreditCardMeta | LoanMeta | InstallmentMeta;
}

interface CreditCardMeta {
  limit: number;
  billDay: number;      // 每月几号账单日
  dueDay: number;       // 每月几号还款日
  tempLimit?: number;   // 临时额度
}

interface LoanMeta {
  principal: number;
  annualRate: number;
  termMonths: number;
  startDate: string;
  repaymentMethod: 'equal_interest' | 'equal_principal' | 'interest_only' | 'bullet';
  monthlyPayment: number;
  autoDeduct: boolean;
  deductAccount?: string; // 关联扣款账户ID
}

interface InstallmentMeta {
  totalLimit: number;
  availableLimit: number;
}

// 分期计划（独立表）
interface InstallmentPlan {
  id: string;
  name: string;
  type: 'credit_card' | 'bt' | 'consumer';
  totalAmount: number;
  fee: number;           // 总手续费
  term: number;
  paidTerms: number;
  monthlyPayment: number;
  nextDueDate: string;
  accountId: string;       // 关联账户
  parentTxId?: string;   // 关联原始消费流水
  status: 'active' | 'completed' | 'prepaid';
  autoDeduct: boolean;
}

// 交易流水
interface Transaction {
  id: string;
  date: string;
  time?: string;
  type: TxType; // income | expense | transfer | repayment | refund | adjustment
  amount: number;
  accountId: string;
  category: string;
  subcategory?: string;
  description: string;
  tags: string[];
  relatedAccountId?: string;
  installmentPlanId?: string;
  location?: string;
  attachments?: string[]; // 图片URL
}
```

### 附录B：口语化金额测试用例集

| 输入 | 期望解析 | 难度 |
|------|----------|------|
| "25" | 25.00 | 低 |
| "二十五" | 25.00 | 低 |
| "3k" | 3000.00 | 低 |
| "两千五" | 2500.00 | 低 |
| "三十五块五" | 35.50 | 中 |
| "一万二千三百四十五" | 12345.00 | 中 |
| "3万5" | 35000.00 | 中 |
| "两百来块" | 200.00（标记为估算） | 高 |
| "花了小一千" | 1000.00（标记为估算） | 高 |

---

> **审计结论**：该产品提示词在核心记账场景下设计较为完整，自然语言交互思路清晰，数据模型基本覆盖需求。v1.0 指出的「AI状态维护风险」「账户模型字段冗余」「缺失支付宝/花呗等主流渠道」「缺少撤销和周期性记账」等问题在代码 v2.0 + 2026-07-30 体验调优后已大部分解决：状态管理已重构为「AI 生成指令 + 前端执行」、账户模型已采用 `meta` 扩展（credit/loan/installment）、支付宝/花呗/现金已覆盖、撤销与周期记账已实现。2026-07-30 新增左滑操作、流水默认本月、默认 DeepSeek、体积守卫等体验与工程化改进，进一步收敛了交互噪音与部署风险。剩余 P2 项（趋势分析、理财账户、抖音月付）建议在后续迭代补齐。

---
*文档生成时间：2026-07-24 08:59 CST*  
*最近复核：2026-07-30（v1.2，对齐左滑操作 / 默认本月 / 默认 DeepSeek / 体积守卫）*  
*审计人：AI产品审计助手*
